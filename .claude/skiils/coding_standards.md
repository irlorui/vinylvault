# Coding standards

## Dependencies
Always use `uv add <package>`, never `pip install`.

## Docstrings
Google style. One-liners are ok for simple functions.

## Formatting
Run `uv run ruff format .` to format.
Run `uv run ruff check .` to lint.
